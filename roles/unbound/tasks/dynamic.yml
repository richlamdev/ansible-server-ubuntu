---
- name: Deploy unbound.conf template configuration file if it does not exist
  template:
    src: 00-top-level-server.conf.j2
    dest: "/etc/unbound/unbound.conf.d/00-top-level-server.conf"
    mode: "0644"
    owner: "root"
    group: "root"
  when: unbound_conf_stat.stat.exists == False
  notify: reload unbound
